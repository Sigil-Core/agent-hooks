# Agent Hooks P-12 OIDC publication probe

This temporary package exists only to prove GitHub-hosted OIDC publication
from the protected `release/s12-emergency` branch. It contains no runtime code
and is never published under `latest`.
