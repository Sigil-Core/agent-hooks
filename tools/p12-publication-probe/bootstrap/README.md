# Agent Hooks P-12 bootstrap publication probe

This temporary package exists only to bootstrap and verify the npm publication
trust path for the retained `release/s12-emergency` branch. It contains no
runtime code and is never published under `latest`.
